Place the adapted PENELOPE Fortran sources for the custom `gview3d.exe` here.

Suggested goals for the first modified build:

- accept a geometry filename from stdin or argv consistently
- emit clearer geometry-load diagnostics
- support Studio-friendly launch behavior from a staged session folder
- mirror any useful `.rep` or console diagnostics strategy used by the custom `gview2d`
